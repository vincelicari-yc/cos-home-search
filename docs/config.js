/* Site configuration.
 *
 * apiUrl: leave empty and the page still works — ratings and comments just save to
 * whoever's device made them, and a banner says so. Paste the Google Apps Script
 * /exec URL here (see docs/SHARED-RATINGS-SETUP.md) and ratings become shared across
 * the whole family. Nothing else needs to change.
 */
window.COS_CONFIG = {
  apiUrl: "",
};
